<%- include('./../partials/header.ejs') %> 
<% if (isLoggedIn) { %>
    <%- include('./../partials/BootNav_2.ejs') %>
<% } else{ %>
    <%- include('./../partials/BootNav.ejs') %>
    <% } %>


<link rel="stylesheet" href="/css/bootstrap.css">

<style>
    /* Style the add to cart section */
    .add-to-cart {
        margin-top: 20px;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .quantity-input {
        display: flex;
        align-items: center;
        margin-top: 10px;
        justify-content: center;
    }

    .quantity-input button {
        background-color: #333;
        color: #fff;
        border: none;
        font-size: 16px;
        cursor: pointer;
    }

    .quantity-input input {
        width: 40px;
        text-align: center;
        font-size: 16px;
        border: 1px solid #ddd;
        margin: 0 5px;
        padding: 5px;
    }
</style>

<div class="row">

<% women.forEach(product => { %>
    <div class="Mworld">
        <img src="<%= product.img_url %>" alt="<%= product.product_name %> Image">
        <div class="inside">
            

            <h2>
                <a href="./../viewProduct?p_id=<%= product._id %>" >
                    <%= product.product_name %>
                </a>
            </h2>
            <br>
            <p> Timeless elegance: Discover <%= product.product_name %> for women
                </p>
            <span class="price">₹ <%= product.price %></span>
            <br>
            <!-- <a href="/getOrders">Add to Cart  </a> -->
                 
            <% if (isAdmin) { %>

                <a href="./../admin/editProduct?p_id=<%= product._id %>" class="edit-button">
    
                  <i class="fa fa-pencil" ></i>Edit Product
    
              </a>
    
              &nbsp;
    
              <a href="/deleteProduct?p_id=<%= product._id %>" data-product-id="<%= product._id %>" class="remove-button">Remove Product</a>
    
              <% } %>
              <br>
              <br>

<!--               
              <div><a href="/postCart?q=1&p_id=<%= product._id %>" id="addToCartLink"
                data-pid="<%= product._id %>"> Add to Cart </a></div> -->
        <br>
        <!-- <br>
        <div class="quantity-input">
            <button class="decrement">-</button>
            <input type="text" id="quantity" name="quantity" value="1">
            <button class="increment">+</button>
        </div> -->
          </div>
    </div>
  <% }); %>

</div>

<%- include('./../partials/footer.ejs') %> 
    
    